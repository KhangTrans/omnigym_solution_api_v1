export enum ApplicationStatus {
  Draft = "draft",
  Pending = "pending",
  Approved = "approved",
  Rejected = "rejected",
}

export enum CertificateStatus {
  Pending = "pending",
  Approved = "approved",
  Rejected = "rejected",
}
